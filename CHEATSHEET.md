# Claude Code 速查表（Cheat Sheet）

> 一页纸搞定高频操作。建议置顶或打印。

## 启动与退出

| 操作 | 命令 / 按键 |
|------|------------|
| 启动 | 在项目目录运行 `claude` |
| 带任务启动 | `claude "帮我修复登录 bug"` |
| 退出 | `/exit` 或连按两次 `Ctrl + C` |
| 继续上次会话 | `claude --continue` |
| 恢复某次历史会话 | `claude --resume` |

## 常用斜杠命令（在对话框里输入 `/`）

| 命令 | 作用 |
|------|------|
| `/help` | 查看帮助和所有命令 |
| `/clear` | 清空当前对话上下文（开始新任务前常用） |
| `/init` | 自动分析项目并生成 `CLAUDE.md` |
| `/config` | 打开设置（主题、模型等） |
| `/model` | 切换模型 |
| `/permissions` | 查看 / 修改权限规则 |
| `/memory` | 编辑项目记忆 `CLAUDE.md` |
| `/agents` | 管理子代理（subagents） |
| `/mcp` | 管理 MCP 服务器 |
| `/cost` | 查看本次会话消耗 |
| `/review` | 审查代码改动 |
| `/vim` | 开启 vim 编辑模式 |

## 快捷键

| 按键 | 作用 |
|------|------|
| `Esc` | 打断 Claude 当前的操作 |
| `Esc` `Esc` | 编辑并回退到之前的某条消息 |
| `Shift + Tab` | 切换权限模式（普通 / 自动接受 / 计划模式） |
| `Ctrl + C` | 取消当前输入 / 退出 |
| `Ctrl + L` | 清屏 |
| `↑` / `↓` | 翻看历史输入 |
| `@` | 引用文件（输入 `@` 触发文件名补全） |
| `#` | 快速把一条说明追加进 CLAUDE.md（记忆） |
| `!` | 进入 bash 模式，直接敲 shell 命令 |

## 三种权限模式（用 `Shift + Tab` 循环切换）

| 模式 | 行为 | 适用场景 |
|------|------|----------|
| 普通模式 | 每次危险操作都询问你 | 默认、最安全 |
| 自动接受（auto-accept）| 自动批准文件编辑 | 信任任务、想提速时 |
| 计划模式（plan mode）| 只读、先出方案不动手 | 大改动前先对齐思路 |

## 高频提问模板

```
# 读懂代码
解释一下 src/auth.py 是怎么做用户认证的

# 改 bug
登录时报错 "KeyError: token"，帮我定位并修复

# 加功能（建议配合计划模式）
给用户列表加一个按注册时间排序的功能，先给我方案

# 写测试
给 calculator.py 里的函数补一套单元测试

# Git
把刚才的改动提交，commit message 用中文描述清楚
```

## 实用技巧

- **开新任务前先 `/clear`**：上下文太长会变慢、变贵、容易跑偏。
- **大改动先用计划模式**（`Shift + Tab` 切到 plan mode）：让它先给方案，你确认后再动手。
- **善用 `@` 引用文件**：比起描述"那个登录文件"，直接 `@src/auth.py` 更准。
- **让它自己验证**：改完代码加一句"跑一下测试确认通过"。
- **不确定就问它**：`这个项目用的是什么测试框架？` 它会自己去查。
- **用 `#` 沉淀规范**：发现它老犯同一个错，用 `#` 把规则写进 CLAUDE.md，下次它就记得了。

---

📖 不清楚某个功能？回到 [README 学习路线图](README.md) 找对应章节。
