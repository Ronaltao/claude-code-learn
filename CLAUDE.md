# CLAUDE.md —— 本项目的"项目记忆"示范

> 这个文件本身就是教学内容的一部分。
> Claude Code 启动时会自动读取项目根目录的 `CLAUDE.md`，把里面的内容当作"长期记忆"。
> 你正在看的就是一份真实可用的示范，第 08 节会专门讲怎么写好它。

## 项目信息

- 创作者：czt
- 创作时间：2026-06-04

## 这个项目是什么

这是一个**教新手认识 Claude Code 的学习项目**，主要由中文 Markdown 教程、动手练习和一个供练习的示范小程序组成。它**不是**一个需要构建/部署的软件项目。

## 目录结构

- `README.md` —— 学习路线图，一切的入口
- `docs/` —— 14 篇循序渐进的教程，文件名以编号开头
- `exercises/` —— 动手练习；其中 `sample-app/` 是一个**故意留了 bug** 的 Python 小程序，供学员练手
- `CHEATSHEET.md` —— 命令速查表

## 写作约定（修改文档时请遵守）

- 全部使用**简体中文**，面向零基础新手，语气友好、避免黑话；首次出现的英文术语要给中文解释。
- 每篇 `docs/` 教程遵循固定结构：**是什么 → 为什么 → 怎么用 → 动手试一试**。
- 文件内引用其它文档用相对路径的 Markdown 链接，方便点击跳转。
- 代码块要标注语言；命令行示例统一用 `bash`。

## 重要提醒

- `exercises/sample-app/` 里的 bug 是**有意保留**的教学素材，**不要主动去修复它们**，除非用户明确要求演示修复过程。
- 这是纯学习项目，没有 CI、没有依赖安装步骤；示范程序只用 Python 标准库，可直接 `python` 运行。
